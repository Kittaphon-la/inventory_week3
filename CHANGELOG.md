# Changelog

การเปลี่ยนแปลงสำคัญของโครงการ Inventory System บันทึกไว้ในไฟล์นี้

## [2.0.0] - 2026-07-13

### Added
- เพิ่มระบบตรวจสอบข้อมูลจำนวนสินค้าและราคา เพื่อป้องกันโปรแกรมหยุดทำงานเมื่อผู้ใช้กรอกผิดประเภท
- เพิ่ม Automated Regression Test ด้วย PyTest
- เพิ่มการทดสอบ Add, Update, Cut Stock, Low Stock, Summary และ JSON Persistence
- เพิ่มคำเตือนเมื่อสินค้าเหลือน้อยกว่า 5 ชิ้น
- เพิ่มการรองรับไฟล์ `data.json` รูปแบบเดิมที่ใช้คีย์ `n`, `q`, `p`, `c`

### Changed
- Refactor ฟังก์ชันขนาดใหญ่ใน `main()` ออกเป็นโมดูลย่อย
- เปลี่ยนชื่อตัวแปรจาก `x`, `a`, `b`, `c`, `d`, `e` เป็นชื่อที่สื่อความหมาย
- เปลี่ยนชื่อเมนู `Check Check` เป็น `Check Inventory`
- ปรับรูปแบบ JSON ให้ใช้ชื่อคีย์ `name`, `qty`, `price`, `category`
- เปลี่ยนการบันทึกไฟล์เป็นการเขียนไฟล์ชั่วคราวก่อนแทนที่ไฟล์จริง เพื่อลดความเสี่ยง Data Corruption

### Fixed
- ป้องกัน `ValueError` จากการกรอกจำนวนสินค้าและราคาไม่ถูกต้อง
- ป้องกันการตัดสต็อกด้วยค่าศูนย์หรือค่าติดลบ
- ป้องกันจำนวนสินค้าและราคาติดลบ
- ป้องกันการบันทึกข้อมูลที่ไม่มี Product ID, Name หรือ Category

## [1.0.0] - Baseline

### Added
- แสดงรายการสินค้า
- เพิ่มหรืออัปเดตสินค้า
- ตัดสต็อก
- คำนวณมูลค่าคงคลัง
- แจ้งเตือนสินค้าใกล้หมด
- บันทึกข้อมูลลง `data.json`
